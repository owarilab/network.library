echo("read settings.conf\n");

// server listen port
server_port=4445;

// scheduler mode: high, middle, low
scheduler_mode='middle';

// max connection number
max_connection=10;

// ---------------------------------------------------------------
// Agent settings
// ---------------------------------------------------------------

// Maximum ReAct loop iterations per /api/agent/run request
// agent_max_iterations=5;

// ---------------------------------------------------------------
// LLM model settings (override with environment variables)
// Set QS_LLM_MODEL_PATH to the path of a GGUF model file.
// Set QS_LLM_MAX_TOKENS to limit generated tokens (default 512).
// Set QS_LLM_N_CTX to set context window size (default 2048).
// Set QS_AGENT_PROMPT_FILE to override the prompt template file
//   (default: ./prompt.conf).
// ---------------------------------------------------------------

