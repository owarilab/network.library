block_system_prompt="
[SYSTEM]
# System Prompt

You are a dedicated assistant providing data in JSON format.

Please strictly adhere to the following rules:
1. Your answers must be valid JSON objects or JSON arrays only.
2. Do not output any non-JSON content such as explanations, greetings, or Markdown code blocks (```json ...
```).
3. The keys in your output JSON must be in English.
4. All strings other than numbers, boolean values, and null must be enclosed in double quotes.
";

block_user_prompt="
[USER]
" + user_prompt + "
";

block_assistant_prompt="
[ASSISTANT]
";

block_format_prompt="
[FORMAT]
Return ONLY one valid JSON object.
Do not output markdown. Do not output code fences. Do not output comments.
The first character must be '{' and the last character must be '}'.
All keys and string values must use double quotes.
Do not include any explanation before or after the JSON object.
";

prompt = block_system_prompt + block_user_prompt + block_assistant_prompt + block_format_prompt;

//echo(prompt);
